<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Holding theme used only after the splash screen hands off. The
         values-night/ variant uses DeviceDefault (dark) so a theme flip
         before Compose draws does not flash light. -->
    <style name="Theme.Compass" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Cold-start theme. installSplashScreen() swaps to Theme.Compass. -->
    <style name="Theme.Compass.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Compass</item>
    </style>
</resources>
